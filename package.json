{
	"scripts": {
		"dev": "vuepress dev docs",
		"build": "vuepress build docs",
		"deploy-gh": "GH=1 yarn run build && bash scripts/deploy-gh.sh"
	},
	"devDependencies": {
		"vuepress": "^1.5.4"
	}
}
